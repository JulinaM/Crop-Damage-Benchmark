# @package _global_
defaults:
  - /base
  - _self_

# Non-FM baseline: same task/data/protocol as terramind_flood.yaml, encoder
# swapped for a from-scratch U-Net. Per CLAUDE.md sec.2/7: run this before
# treating any foundation-model number as meaningful -- it's the floor that
# tells you whether a foundation model is actually adding value on this hazard.
task: Segmentation
experiment_name: ${task}_unet_flood

# Label class_remap and model.{ignore_index,negative_class,positive_class} are
# tied together -- see crop_damage/datasets/AgDamageShardDataset.py header for
# how the raw label.tif band-1 values map to these ids.
train_loader:
  dataset: agdamage_shard
  data_root: data/input/J_AgDamage/AgDamage_v2
  hazards: [Flood]
  modalities:
    S2L2A: {}
    S1GRD: {}
  label_band: 1
  label_nodata: 255
  class_remap: {1: 2, 2: 1, 3: 0, 4: 0}
  num_augmentations: 5
  patch_size: 224
  preload: False
  batch_size: 4
  shuffle: True
  num_workers: 2

validation_loader:
  dataset: agdamage_shard
  data_root: data/input/J_AgDamage/AgDamage_v2
  hazards: [Flood]
  modalities:
    S2L2A: {}
    S1GRD: {}
  label_band: 1
  label_nodata: 255
  class_remap: {1: 2, 2: 1, 3: 0, 4: 0}
  patch_size: 224
  preload: False
  batch_size: 4
  shuffle: False
  num_workers: 2

# Named eval sets: in-distribution test split for the trained hazard(s), plus
# a leave-one-hazard-out (LOHO) cross-hazard OOD eval, per CLAUDE.md's
# generalization protocol. Add/remove entries freely -- build_holdout_loaders()
# in main.py iterates this dict by name, it's not hardcoded to any names.
eval_loaders:
  in_distribution:
    dataset: agdamage_shard
    data_root: data/input/J_AgDamage/AgDamage_v2
    hazards: [Flood]
    split: test
    modalities:
      S2L2A: {}
      S1GRD: {}
    label_band: 1
    label_nodata: 255
    class_remap: {1: 2, 2: 1, 3: 0, 4: 0}
    patch_size: 224
    num_workers: 2
  loho_burnt:
    dataset: agdamage_shard
    data_root: data/input/J_AgDamage/AgDamage_v2
    hazards: [Burnt]
    split: test
    modalities:
      S2L2A: {}
      S1GRD: {}
    label_band: 1
    label_nodata: 255
    class_remap: {1: 2, 2: 1, 3: 0, 4: 0}
    patch_size: 224
    num_workers: 2

model:
  num_classes: 3
  positive_class: 2
  negative_class: 1
  ignore_index: 0
  learning_rate: 0.0005
  num_epochs: 8
  seed: 22

encoder:
  name: UNet
  modalities: ["S2L2A", "S1GRD"]
  finetune: true  # trained from scratch -- always in the optimizer regardless of this flag, kept true for clarity
  build_kwargs:
    channels: [64, 128, 256, 512, 1024]

decoder:
  name: UNet
  channels: [64, 128, 256, 512, 1024]

trainer:
  n_epochs: 2
  log_interval: 1

use_wandb: False

criterion:
  name: ce #dice_focal, ce
  apply_weight_loss: False
  weight_type: inverse
  dice_weight: 0.5
  focal_weight: 0.5
  focal_gamma: 2.0
  focal_alpha: null
  dice_smooth: 1e-6

change:
  method: attention_based_cross_time_fusion # concatenate_all_three, siamese_fusion, attention_based_cross_time_fusion, signed_difference_plus_absolute_difference
  attention_heads: 4
